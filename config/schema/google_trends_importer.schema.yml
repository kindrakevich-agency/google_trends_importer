google_trends_importer.settings:
  type: config_object
  label: 'Google Trends Importer Settings'
  mapping:
    openai_api_key:
      type: string
      label: 'OpenAI API Key'
    trends_url:
      type: string
      label: 'Google Trends RSS URL'
    # Add the new prompt setting definition
    openai_prompt:
      type: text # Use 'text' for potentially long prompts
      label: 'OpenAI Prompt Template'